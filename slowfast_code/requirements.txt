numpy
torch==1.3.1
torchvision==0.4.2
# git+https://github.com/facebookresearch/fvcore # disable this, else it tries to install it everytime
simplejson
av
PyYaml
tqdm
psutil
opencv-python
cython
joblib
scikit-learn
#git+git://github.com/facebookresearch/detectron2.git@v0.1.1 # disable this, else it tries to install it everytime
#git+https://github.com/cocodataset/cocoapi.git#subdirectory=PythonAPI # disable this, else it tries to install it everytime
# Make sure to install python3-dev and gcc (python3.7-dev in our case)